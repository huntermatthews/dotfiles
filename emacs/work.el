;; This file is for anything that work requires in an emacs config.
;; 
;; This always turns into some kind of fiasco
;; Different at $CORP, but always a fiasco

;; show trailing whitespace ...
;(set-face-background 'trailing-whitespace "#900000")
;(setq-default show-trailing-whitespace t)
; delete-trailing-whitespace

;; MUST HAVE MATCHING TERM SETUP IN .ZSHRC!!
(setq-default tramp-terminal-type "tramp")


;; END OF LINE ;;
